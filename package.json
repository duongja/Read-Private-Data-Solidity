{
  "name": "read-private-data-solidity",
  "version": "1.0.0",
  "private": true,
  "description": "Educational Hardhat project showing why Solidity private storage is still readable on-chain.",
  "scripts": {
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "check": "npm run clean && npm run compile && npm test"
  },
  "keywords": [
    "solidity",
    "hardhat",
    "ethereum",
    "security",
    "storage"
  ],
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "6.1.2",
    "chai": "4.5.0",
    "ethers": "6.16.0",
    "hardhat": "2.28.6"
  }
}
